# Hỗ trợ cả 2 kiến trúc 32-bit và 64-bit phổ biến trên Android hiện nay
APP_ABI := armeabi-v7a arm64-v8a

APP_PLATFORM := android-21

APP_STL := c++_static

APP_CPPFLAGS := -std=c++17

APP_OPTIM := release
